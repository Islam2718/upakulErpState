import { OfficeComponentMapping } from '../office-component-mapping';

describe('OfficeComponentMapping', () => {
  it('should create an instance', () => {
    expect(new OfficeComponentMapping()).toBeTruthy();
  });
});
